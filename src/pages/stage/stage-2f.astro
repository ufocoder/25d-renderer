---
import PageLayout from '@app/layouts/PageLayout.astro';
import type { PageMetadata } from '@app/config/pages';
import { codeToHtml } from 'shiki';

export const metadata: PageMetadata = {
  title: 'Порталы: соединяем сектора',
  groupTitle: 'Stage 2: Отрисовка секторов',
};

const code1 = `
  interface Seg {
    // ..
    frontSector: Sector;
    backSector: Sector;
    isTwoSide: boolean;
  }

  function isPortal(seg: Seg): boolean {
    return Boolean(seg.isTwoSide && seg.backSector && seg.backSector !== seg.frontSector);
  }

`;

const code1Html = await codeToHtml(code1, { lang: 'ts', theme: 'github-dark' });
---

<PageLayout>
  <section class="flex flex-col gap-4">
    <p class="my-2">
      Интуиция подсказываем нам, что с общими стенами между секторами необходимо что-то сделать. Расширим понимание сегмента. Пусть теперь будут встречаться и такие, которые мы в будущем не закрашиваем, но через которые будет рисоваться оставшийся уровень. Назовем такие сегменты — порталами.
    </p>
    <p>
      Сегменты, образующие порталы, будем помечать флагом и хранить в нем информацию о том, какие сектора они соединяют:
    </p>
    <div set:html={code1Html} />
    <p>
      Затем мы рекурсивно обработаем сектора, в которых перебираем сегменты и формируем проекцию с учетом знаний о портале.
    </p>
  </section>
</PageLayout>
